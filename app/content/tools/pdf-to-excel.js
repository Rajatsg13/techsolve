const content = {
  slug: 'pdf-to-excel',
  outcome: 'The tables from your PDF as a real .xlsx workbook you can sort, filter and total.',
  whatItDoes: [
    'It reads the text layer inside a PDF, works out which parts of each page are laid out as a grid, and writes each table to its own sheet in an Excel workbook. Values that are genuinely numeric arrive as numbers, so they can be summed straight away.',
    'A table that continues across a page break is joined back together, and a repeated header row on the second page is dropped. Only tables on consecutive pages with the same number of columns are joined, so two unrelated tables are never silently merged.',
  ],
  limitations: [
    'This works on PDFs generated by software — exports, statements, system reports. It reads the text a PDF already carries.',
    'It cannot read scanned documents or tables saved as pictures, because there is no text in them to read. If a document has no text layer, you are told that rather than being given an empty workbook.',
    'Complex magazine-style layouts, tables without consistent column positions, and heavily merged cells will not extract reliably. Where nothing table-shaped is found, nothing is invented.',
  ],
  whenToUse: [
    { title: 'You need to do arithmetic on data stuck in a PDF', body: 'Totals, sorting and filtering are all impossible until the numbers are in a spreadsheet.' },
    { title: 'Retyping would take an hour', body: 'The usual alternative to this tool, and the one that introduces mistakes.' },
    { title: 'A system only exports PDF', body: 'Plenty of older business software offers a printable report and nothing else.' },
  ],
  workplaceUses: [
    { title: 'Bank and card statements', body: 'Get transactions into a sheet for reconciliation or expense categorisation.' },
    { title: 'Supplier invoices and purchase orders', body: 'Pull line items out to compare against what was ordered or received.' },
    { title: 'Reports from internal systems', body: 'Turn a fixed PDF report into something you can pivot and chart.' },
    { title: 'Price lists and catalogues', body: 'Bring a supplier’s list into a sheet to compare against your own.' },
  ],
  howToSteps: [
    { title: 'Add your PDF', body: 'Up to 50 MB and 100 pages. Each page is analysed as it loads.' },
    { title: 'Look at what was found', body: 'Every detected table is previewed with its page number, row and column counts, and the first few rows.' },
    { title: 'Decide about joining', body: 'Tables continuing across pages are joined by default. Turn it off if you would rather each page stayed separate.' },
    { title: 'Check the preview before downloading', body: 'This is the moment to spot a table that has been read badly — much easier here than after opening the workbook.' },
    { title: 'Download the .xlsx', body: 'Each table becomes its own sheet, named with the pages it came from.' },
  ],
  tips: [
    { title: 'Test whether your PDF has a text layer', body: 'Open it in a reader and try to select a word inside the table. If you cannot, the page is an image and this tool cannot read it.' },
    { title: 'Always check the preview', body: 'Table extraction is inference, not a guaranteed conversion. A misread column is obvious in the preview and easy to miss in a workbook.' },
    { title: 'Identifiers stay text on purpose', body: 'Values like 007 or a reference number are left as text, because converting them would lose the leading zero. Only unambiguous numbers become numbers.' },
    { title: 'Ruled lines do not help', body: 'Detection works from where the text sits, not from drawn borders. A table with no lines can extract perfectly; one with heavy borders can still be missed if its columns do not line up.' },
    { title: 'For a scan, OCR first', body: 'Running OCR adds a text layer that this tool can then read — but OCR output is a best guess, so check the numbers carefully.' },
  ],
  faqs: [
    { q: 'Why did it find no tables in my PDF?', a: 'Either the document has no text layer, which means it is a scan or images and there is nothing to read, or its content is not laid out as a consistent grid. You are told which of the two it is. Nothing is guessed at, because an invented table is worse than none.' },
    { q: 'Does it work on scanned documents?', a: 'No. A scan is a picture of a page with no text inside it. Run it through OCR PDF first to add a text layer, then try again — and check the results, because OCR makes mistakes.' },
    { q: 'Is the output a real Excel file?', a: 'Yes, a genuine .xlsx workbook. It opens in Excel, LibreOffice, Numbers and Google Sheets, with numbers stored as numbers rather than text.' },
    { q: 'Why is one table split across two sheets?', a: 'Joining only happens for tables on consecutive pages with the same number of columns. If a column was misread on one page, the shapes differ and the tables stay separate — a deliberately cautious rule, because wrongly merging two tables is harder to notice than a split one.' },
    { q: 'Are formatting, colours and merged cells kept?', a: 'No. This extracts values, not appearance. You get the data with a bold header row and nothing else.' },
    { q: 'Is my document uploaded?', a: 'No. The PDF is read and the workbook is built in your browser, and nothing is stored after you close the tab.' },
  ],
  relatedWorkflows: [
    {
      title: 'Get data out of a scanned report',
      description: 'When the PDF is an image, add a text layer first.',
      steps: [
        { slug: 'pdf-ocr', note: 'Add a text layer with OCR' },
        { slug: 'pdf-to-excel', note: 'Extract the tables' },
        { label: 'Check the numbers against the original' },
      ],
    },
    {
      title: 'Pull one table out of a long report',
      description: 'Narrow the document down before extracting.',
      steps: [
        { slug: 'pdf-split', note: 'Keep only the pages with the table' },
        { slug: 'pdf-to-excel', note: 'Convert those pages' },
      ],
    },
  ],
  relatedTools: ['pdf-to-word', 'pdf-ocr', 'pdf-split', 'pdf-merge'],
};
export default content;
